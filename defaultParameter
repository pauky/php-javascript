//函数默认参数
javascript:

function test(a,b,c) {
  a = a ? a : 1;
  b = b ? b : 2;
  c = c ? c : 3;
  alert(a+"-----"+b+"-----"+c);
}
test();//1----2----3


PHP:

function test($a = 1,$b = 2,$c = 3) {
  echo $a,$b,$c;
}
test();//123
